namespace = study_knowledge_entry

######################
# Study Knowledge Entry Outcomes
# 0001-0002
######################

# Success!
study_knowledge_entry.0001 = {
	type = character_event
	title = study_knowledge_entry.0001.t
	desc = study_knowledge_entry.0001.desc
	theme = education
	left_portrait = {
		character = root
		animation = happiness
	}
	artifact = {
		target = scope:scheme.var:required_artifact
		position = lower_center_portrait
		trigger = { exists = scope:scheme.var:required_artifact }
	}

	override_background = {
		trigger = { is_travelling = yes }
		reference = terrain_travel
	}

	option = {
		name = study_knowledge_entry.0001.a

		send_interface_toast = {
			title = study_knowledge_entry_complete
			left_icon = root
			knowledge_entry_bonus_table_effect = { MODE = learn }
			scope:scheme = { end_scheme = yes }
		}
		
		ai_chance = {
			base = 100
		}
	}
}

# Failure!
study_knowledge_entry.0002 = {
	type = character_event
	title = study_knowledge_entry.0002.t
	desc = study_knowledge_entry.0002.desc
	theme = education
	left_portrait = {
		character = root
		animation = rage
	}
	artifact = {
		target = scope:scheme.var:required_artifact
		position = lower_center_portrait
		trigger = { exists = scope:scheme.var:required_artifact }
	}

	override_background = {
		trigger = { is_travelling = yes }
		reference = terrain_travel
	}

	option = {
		name = study_knowledge_entry.0002.a

		scope:scheme = {
			end_scheme = yes
		}
		
		ai_chance = {
			base = 100
		}
	}
	
	option = {
		name = study_knowledge_entry.0002.b
		add_stress = major_stress_gain
		custom_tooltip = scheme_loses_half_progress
		hidden_effect = {
			scope:scheme = {
				add_scheme_progress = -5
			}
		}
	}
}
