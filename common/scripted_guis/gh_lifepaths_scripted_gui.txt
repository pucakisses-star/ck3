has_lifepaths_scripted_gui = {
	scope = faith
	
	is_shown = {
		trigger_if = {
			limit = { has_variable = number_of_lifepaths }
			var:number_of_lifepaths > 0
		}
		trigger_else = { always = no }
	}
	
	effect = {
		
	}
}