

bastard_legitimacy_change_cutoff_age = 6

real_father_suspicion_base_value = 50
husband_suspicion_base_value = 3

bastard_legitimization_prestige_cost = { value = GH_bastard_legitimization_prestige_cost }

house_head_legitimize_acceptance_base_value = -100

house_head_legitimize_approval_rating = {
	value = house_head_legitimize_acceptance_base_value

	if = {
		limit = {
			scope:the_house_head = scope:the_legitimizer
		}
		add = 1000
	}

	add = {
		value = scope:the_legitimizer.prestige_level
		multiply = 20
	}

	if = {
		limit = {
			scope:the_house_head = {
				opinion = {
					target = scope:the_legitimizer
					value >= 50
				}
			}
		}
		add = 51
	}
	if = {
		limit = {
			scope:the_house_head = {
				opinion = {
					target = scope:the_bastard
					value >= 80
				}
			}
		}
		add = 51
	}
	if = {
		limit = {
			scope:the_house_head = {
				opinion = {
					target = scope:the_legitimizer
					value <= 0
				}
			}
		}
		add = -30
	}
	if = { # GH Addition: For the reduced bastard cost cultural param
		limit = {
			scope:the_legitimizer.culture = {
				has_cultural_parameter = reduced_cost_legitimization
			}
		}
		subtract = miniscule_dynasty_prestige_value
	}
}

GH_bastard_legitimization_prestige_cost = { # GH Addition: For the reduced bastard cost cultural param
	value = 150

	if = {
		limit = {
			scope:root.culture = {
				has_cultural_parameter = reduced_cost_legitimization
			}
		}
		subtract = 150
	}
}