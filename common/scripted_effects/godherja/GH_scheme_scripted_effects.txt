gh_laamp_base_1001_contract_scheme_prep_effect = {
	save_scope_as = scheme
	scope:scheme.task_contract ?= { save_scope_as = task_contract }
	save_scope_value_as = {
		name = follow_up_event
		value = event_id:gh_scheme_critical_moments.1001
	}
	if = {
		limit = {
			NOT = { exists = scope:suppress_next_event }
		}
		scheme_owner = { trigger_event = scheme_critical_moments.0002 }
	}
}

gh_laamp_base_1021_contract_scheme_prep_effect = {
	save_scope_as = scheme
	scope:scheme.task_contract ?= { save_scope_as = task_contract }
	save_scope_value_as = {
		name = follow_up_event
		value = event_id:gh_scheme_critical_moments.1021
	}
	if = {
		limit = {
			NOT = { exists = scope:suppress_next_event }
		}
		scheme_owner = { trigger_event = scheme_critical_moments.0002 }
	}
}

gh_laamp_base_1041_contract_scheme_prep_effect = {
	save_scope_as = scheme
	scope:scheme.task_contract ?= { save_scope_as = task_contract }
	save_scope_value_as = {
		name = follow_up_event
		value = event_id:gh_scheme_critical_moments.1041
	}
	if = {
		limit = {
			NOT = { exists = scope:suppress_next_event }
		}
		scheme_owner = { trigger_event = scheme_critical_moments.0002 }
	}
}

## Migration Contracts
gh_laamp_base_9001_contract_scheme_prep_effect = {
	save_scope_as = scheme
	scope:scheme.task_contract ?= { save_scope_as = task_contract }
	save_scope_value_as = {
		name = follow_up_event
		value = event_id:gh_scheme_critical_moments.9001
	}
	if = {
		limit = {
			NOT = { exists = scope:suppress_next_event }
		}
		scheme_owner = { trigger_event = scheme_critical_moments.0002 }
	}
}

gh_laamp_base_9011_contract_scheme_prep_effect = {
	save_scope_as = scheme
	scope:scheme.task_contract ?= { save_scope_as = task_contract }
	save_scope_value_as = {
		name = follow_up_event
		value = event_id:gh_scheme_critical_moments.9011
	}
	if = {
		limit = {
			NOT = { exists = scope:suppress_next_event }
		}
		scheme_owner = { trigger_event = scheme_critical_moments.0002 }
	}
}

#gh_laamp_base_9031_contract_scheme_prep_effect = {
	#save_scope_as = scheme
	#scope:scheme.task_contract ?= { save_scope_as = task_contract }
	#save_scope_value_as = {
		#name = follow_up_event
		#value = event_id:gh_scheme_critical_moments.9031
	#}
	#if = {
		#limit = {
			#NOT = { exists = scope:suppress_next_event }
		#}
		#scheme_owner = { trigger_event = scheme_critical_moments.0002 }
	#}
#}
#gh_laamp_base_9041_contract_scheme_prep_effect = {
	#save_scope_as = scheme
	#scope:scheme.task_contract ?= { save_scope_as = task_contract }
	#save_scope_value_as = {
		#name = follow_up_event
		#value = event_id:gh_scheme_critical_moments.9041
	#}
	#if = {
		#limit = {
			#NOT = { exists = scope:suppress_next_event }
		#}
		#scheme_owner = { trigger_event = scheme_critical_moments.0002 }
	#}
#}