# character having its birthday in root scope
on_birthday_magic_education = {
	trigger = {
		has_trait = magic_good
	}
	events = {
		court_magi_task.0100 # all this does is activate childhood_magic_education_base_effect, but doing this way since I think it might prevent wonky random chances
	}
}