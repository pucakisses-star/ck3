GH_on_county_culture_change = {
	effect = {
		if = { #Removes lodge modifiers
			limit = {
				scope:old_culture = {
					has_cultural_parameter = unlocks_lodge_mechanics
				}
				culture = { NOT = { has_cultural_parameter = unlocks_lodge_mechanics } }
			}
			remove_lodges_effect = yes
		}
	}
}